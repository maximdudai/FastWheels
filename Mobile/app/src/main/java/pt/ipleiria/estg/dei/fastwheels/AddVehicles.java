/*package pt.ipleiria.estg.dei.fastwheels;

import android.os.Bundle;
import android.widget.Button;
import android.widget.EditText;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

public class AddVehicles extends AppCompatActivity {

    private EditText vehicleBrand, vehicleModel, vehicleYear, vehicleDoors, locationCity, locationX, locationY, availableFrom, availableTo;
    private Button btnAdd;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_vehicles);

        vehicleBrand = findViewById(R.id.etMarca);
        vehicleModel = findViewById(R.id.etModelo);
        vehicleYear = findViewById(R.id.etAno);
        vehicleDoors = findViewById(R.id.etNumPortas);
        locationCity = findViewById(R.id.etCidade);
        locationX = findViewById(R.id.etLocalizacaoX);
        locationY = findViewById(R.id.etLocalizacaoY);
        availableFrom = findViewById(R.id.etDisponivelDe);
        availableTo = findViewById(R.id.etDisponivelAte);
        btnAdd = findViewById(R.id.btnAdicionar);

        });
    }
}*/